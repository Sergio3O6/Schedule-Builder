// Placeholder shell. Commit 1 exists to prove the toolchain, not to build UI —
// real components arrive after the domain model and solver land.
export function App() {
  return (
    <main>
      <h1>KU Schedule Builder</h1>
    </main>
  )
}
